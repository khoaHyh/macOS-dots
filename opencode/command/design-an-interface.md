---
description: Generate and compare multiple interface designs for a module
argument-hint: "[module, API, or interface to design]"
---

Run the `design-an-interface` workflow for this module or API.

First, invoke the skill tool to load the workflow:

```text
skill({ name: 'design-an-interface' })
```

Then follow the skill instructions exactly.

<user-request>
$ARGUMENTS
</user-request>
