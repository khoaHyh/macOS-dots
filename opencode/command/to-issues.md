---
description: Break a plan, spec, or PRD into vertical-slice issues
argument-hint: "[plan, spec, PRD, issue reference, or extra steering]"
---

Run the `to-issues` workflow for the current plan, spec, PRD, or provided issue reference.

First, invoke the skill tool to load the workflow:

```text
skill({ name: 'to-issues' })
```

Then follow the skill instructions exactly.

<user-request>
$ARGUMENTS
</user-request>
