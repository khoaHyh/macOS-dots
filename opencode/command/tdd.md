---
description: Build or fix behavior with a test-driven red-green-refactor loop
argument-hint: "[feature, bug, or behavior to implement with TDD]"
---

Run the `tdd` workflow for this feature, bug, or behavior.

First, invoke the skill tool to load the workflow:

```text
skill({ name: 'tdd' })
```

Then follow the skill instructions exactly.

<user-request>
$ARGUMENTS
</user-request>
