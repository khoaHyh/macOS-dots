---
description: Address an existing set of pull-request review feedback
---

First, invoke the skill tool to load the workflow:

```text
skill({ name: 'review-remediation' })
```

Then follow the skill instructions exactly.
