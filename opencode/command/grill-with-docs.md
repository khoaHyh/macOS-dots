---
description: Stress-test a plan against domain docs and update context as decisions settle
argument-hint: "[plan, design, or decision to grill against repo docs]"
---

Run the `grill-with-docs` workflow for this plan or design.

First, invoke the skill tool to load the workflow:

```text
skill({ name: 'grill-with-docs' })
```

Then follow the skill instructions exactly.

<user-request>
$ARGUMENTS
</user-request>
