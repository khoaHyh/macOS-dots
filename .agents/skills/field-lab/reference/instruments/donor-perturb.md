---
id: donor-perturb
name: "Donor perturbation rig"
summary: "Distant donor mechanisms, mappings, fit, and transfer limits"
use_when: "The home field lacks a needed mechanism"
avoid_when: "Do not claim blind donor transfer without an abstract home-free brief, isolated donor research, and a nearby failure case."
access_target: "Distant donor mechanisms, mappings, fit, and transfer limits"
requires: "an abstract missing capacity or structural pattern"
execution_seat: hybrid
fresh_context: required
effort: high
persistence: "Several research agents; offer a Field Log to keep sources, mappings, and donor pages together."
artifact_risk: "Elegant false isomorphism, cherry-picked fields, and home concepts smuggled into the donor brief."
maturity: trialed
documented_uses: 8
---

# Donor perturbation rig (`donor-perturb`)

- **Phenomenon / range / input:** Transferable mechanisms outside the home domain; needs an abstract missing capacity or structural pattern.
- **Why use it:** Search from the home problem favors neighboring analogies. Blind recruitment exposes distant mechanisms before familiarity with the home problem can select them.
- **Perturbation / procedure:** Abstract the missing capacity or relation without naming the home domain. Ask a fresh recruiter for several distant fields where that structure matters; let the orchestrator reject only home-adjacent choices. Give each selected field to a separate researcher that sees the donor question but not the home problem. Extract the donor's parts, operations, boundary conditions, and failure cases. Only then reveal the home structure and map the mechanism back. Mark each link `[fit: solid|plausible|reach]` and freeze a nearby case where the transfer should fail.
- **Readout / control:** Donor mechanism, proposed transfer, `[fit:]` strength, analogy boundary, and a nearby negative-control case where the transfer should fail.
- **Common artifacts:** Elegant false isomorphism, cherry-picked fields, and home concepts smuggled into the donor brief.
- **Escalate / stop:** Escalate when a supported transfer changes the decomposition. Stop when no mapping discriminates among nearby cases.
- **What it requires:** Several research agents; offer a Field Log to keep sources, mappings, and donor pages together.
- **Execution placement:** **Hybrid.** A fresh blind recruiter selects distant fields without the home domain; separate research subagents study each donor in its own terms; the sighted orchestrator may veto only home-adjacent picks, then applies the registered fit and negative-transfer checks. These separations prevent home-frame selection. If they are unavailable, downgrade to a sighted analogy search and do not claim blind donor perturbation. Return donor mechanisms, source claims, mappings, fit readings, and failures without incorporating them into a theory.
