---
id: residue-collect
name: "Residue collector"
summary: "Sourced remainder exposed by a named lens"
use_when: "A frame or candidate may have dropped material"
avoid_when: "Do not run without a structured reading and source trace, or invent remainder merely to keep the inquiry open."
access_target: "Sourced remainder exposed by a named lens"
requires: "at least one structured reading"
execution_seat: either
fresh_context: optional
effort: medium
persistence: "One analytical pass; can be done here, or kept in a field log or tension pages when part of larger work."
artifact_risk: "The orchestrator treats personal interest as signal or invents residue to keep the inquiry open."
maturity: trialed
documented_uses: 8
---

# Residue collector (`residue-collect`)

- **Phenomenon / range / input:** Material a frame, comparison, or candidate cannot absorb without loss; needs at least one structured reading.
- **Why use it:** Attention follows what the active result explains. A focused remainder pass makes dropped or incompatible material visible rather than treating it as noise.
- **Perturbation / procedure:** Freeze the active frame, comparison, or candidate. Re-scan its source material through four named lenses: protected claims it cannot absorb, concrete briefing facts it leaves unused, interests or history built into its frame, and terms or ends that remain incompatible. Record only sourced remainder, name the lens that exposed it, and distinguish frame-caused omission from missing input.
- **Readout / control:** Terse residual items with source pointers, the lens that exposed each item, evidence status, and whether the omission came from the active frame or missing input. Do not explain the residue's importance or fold it into a new frame.
- **Common artifacts:** The orchestrator treats personal interest as signal or invents residue to keep the inquiry open.
- **Escalate / stop:** Offer another instrument when a residual item maps to its measurement target. Stop when another remainder pass yields no new sourced item.
- **What it requires:** One analytical pass; can be done here, or kept in a field log or tension pages when part of larger work.
- **Execution placement:** **Either; orchestrator by default.** The executor sees the structured reading and its source trace; a fresh subagent may receive the same material without the orchestrator's preferred result for a separated remainder scan. Separation improves control but is not required. If only the orchestrator runs it, state capture risk. Return the residual items as a separate list; do not integrate them.
