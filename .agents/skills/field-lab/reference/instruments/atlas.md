---
id: atlas
name: "Atlas and field notebook"
summary: "Searchable state, lineage, linked tensions, and gaps"
use_when: "Session memory cannot expose drift, provenance, or cross-links"
avoid_when: "Do not add graph memory when one session or one field log already exposes the needed lineage and gaps."
access_target: "Searchable state, lineage, linked tensions, and gaps"
requires: "An inquiry whose reuse, cross-links, several agents, recursion, or context risk exceeds what one field log can show."
execution_seat: either
fresh_context: optional
effort: high
persistence: "Ongoing agent and file work; use only when a field log no longer exposes the needed links and history."
artifact_risk: "Recorded categories harden into ontology; maintenance displaces inquiry; an elegant graph hides weak support."
maturity: trialed
documented_uses: 8
---

# Atlas and field notebook (`atlas`)

- **Phenomenon / range / input:** Long-running provenance, cross-links, coverage, and inquiry lineage; useful when one conversation no longer shows enough history.
- **Why use it:** A live conversation cannot expose long-term drift, forgotten provenance, or links across later conversations. The organized record makes them inspectable; until then, the notebook is support rather than an instrument.
- **Perturbation / procedure:** Create one authoritative entry record, typed source and reading pages only when reuse warrants them, explicit provenance links, an open-gap list, and a lineage-aware queue. Keep raw sources immutable, append changes instead of silently overwriting them, and let every category be revised. In the Electric Monk workflow, apply the additional page classes and firewall rules in [dialectic-wiki.md](../dialectic-wiki.md).
- **Readout / control:** Searchable state, open gaps, linked tensions, and provenance; retain raw sources and allow categories to be revised.
- **Common artifacts:** Recorded categories harden into ontology; maintenance displaces inquiry; an elegant graph hides weak support.
- **Escalate / stop:** Add graph memory to a Field Trip or Expedition only when reuse, cross-links, several agents, recursion, or context risk warrants it. Stop gardening when the record answers its consumers' needs.
- **What it requires:** Ongoing agent and file work; use only when a field log no longer exposes the needed links and history.
- **Execution placement:** **Either; persistent background gardener subagent by default when several agents are active.** The keeper sees approved records and provenance but not hidden Monk-to-Monk material barred by the firewall. Independence is not epistemic; persistence and workload are the reasons to delegate. Dispatch record work asynchronously and continue; synchronize only when the next operation consumes a requested artifact or a completion gate has no other unfinished work. If no gardener exists, the orchestrator maintains the record directly. The readout is paths, links, provenance, and gaps only.
